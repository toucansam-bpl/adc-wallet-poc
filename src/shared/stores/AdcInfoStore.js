import { action,
         computed, 
         decorate,
         observable,
       } from 'mobx'


export default class AdcInfoStore {
  constructor() {
  }

  async init() {
  }
}

decorate(AdcInfoStore, {
})
